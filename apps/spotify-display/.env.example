# Default Sonos room to monitor for playback when controlling the display
SONOS_ROOM=Living Room

# Hide cursor only while Sonify/Weather is being displayed (requires `unclutter`)
HIDE_CURSOR_WHILE_DISPLAYING=1

# Seconds of mouse idle before hiding cursor while kiosk content is active
HIDE_CURSOR_IDLE_SECONDS=0.1
