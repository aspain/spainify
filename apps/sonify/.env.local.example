# Vue CLI runtime override for Sonify target room
VUE_APP_SONOS_ROOM="Living Room"

# Optional: metadata enrichment endpoint (uses media-actions-api service)
# VUE_APP_MEDIA_ACTIONS_BASE="http://localhost:3030"
# Legacy fallback key (still supported):
# VUE_APP_ADD_CURRENT_BASE="http://localhost:3030"

# Legacy fallback (supported, but VUE_APP_SONOS_ROOM is preferred)
# VITE_SONOS_ROOM="Living Room"
